@model IEnumerable<BonTemps.Models.Table_layout>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LayoutX)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LayoutY)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsTable)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LayoutX)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LayoutY)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsTable)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TableLayoutId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TableLayoutId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TableLayoutId })
        </td>
    </tr>
}

</table>
