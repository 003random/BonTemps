@model IEnumerable<BonTemps.Models.Customers>

@{
    ViewBag.Title = "Klanten overzicht";
}

<div class="col-md-12 padding-0">
    <div class="col-md-12">
        <div class="panel">
            <div class="panel-heading"><h3>Klanten overzicht</h3></div>
            <div class="panel-body">
                <div class="responsive-table">
                    <table id="datatable" class="table table-striped table-bordered" width="100%" cellspacing="0">
                        <thead>
                            <tr>
                                <th>@Html.DisplayNameFor(model => model.Gender)</th>
                                <th>@Html.DisplayNameFor(model => model.FirstName)</th>
                                <th>@Html.DisplayNameFor(model => model.Prefix)</th>
                                <th>@Html.DisplayNameFor(model => model.LastName)</th>
                                <th>@Html.DisplayNameFor(model => model.PhoneNumber)</th>
                                <th>@Html.DisplayNameFor(model => model.Email)</th>
                                <th>@Html.DisplayNameFor(model => model.NewsLetter)</th>
                                <th>Opties</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(modelItem => item.Gender)</td>
                                    <td>@Html.DisplayFor(modelItem => item.FirstName)</td>
                                    <td>@Html.DisplayFor(modelItem => item.Prefix)</td>
                                    <td>@Html.DisplayFor(modelItem => item.LastName)</td>
                                    <td>@Html.DisplayFor(modelItem => item.PhoneNumber)</td>
                                    <td>@Html.DisplayFor(modelItem => item.Email)</td>
                                    <td>@Html.DisplayFor(modelItem => item.NewsLetter)</td>
                                    <td>
                                        <span onclick="window.location = '@Url.Action("Edit", "Customers", new {id = item.Id})';" class="fa fa-pencil-square-o" style="margin-left: 10px; font-size:20px; cursor: pointer"></span>
                                        <span onclick="window.location = '@Url.Action("Details", "Customers", new {id = item.Id})';" class="fa fa-info-circle" style="margin-left: 10px; font-size:20px; cursor: pointer"></span>
                                        <span onclick="window.location = '@Url.Action("Delete", "Customers", new {id = item.Id})';" class="fa fa-times" style="margin-left: 10px; font-size:20px; cursor: pointer"></span>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>

                    @using (Html.BeginForm("ExportToExcel", "Reservation", FormMethod.Post))
                    {
                        <input type="submit" value="Exporteer naar Excel" class="button" />
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script type="text/javascript">
        $(document).ready(function () {
            $('#datatable').DataTable();
        });
    </script>
}